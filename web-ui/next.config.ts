import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // turbopack is default in next.js 16, no config needed
  // reactCompiler: true, // uncomment to enable react compiler
}

export default nextConfig

